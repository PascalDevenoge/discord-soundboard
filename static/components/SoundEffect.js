import { html } from '../deps.js';

const SoundEffect = () => {
    return (
        html`
            <div>
                This is sound effect!
            </div>
        `
    );
}

export default SoundEffect;