import { html } from '../deps.js';

const Settings = () => {
    return (
        html`
            <div>
                This is Settings!
            </div>
        `
    );
}

export default Settings;